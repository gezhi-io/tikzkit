# TikZ Knowledge

当前 TikZ 学习记录的主目录是：

```text
docs/tikz-knowledge/
```

Section 15 已记录在：

```text
docs/tikz-knowledge/10-actions-on-paths.md
```

这个 `doc/knowledge/` 目录只作为入口说明，避免把 `doc` 和 `docs` 看混。后续统一维护位置仍然是 `docs/tikz-knowledge/`。
