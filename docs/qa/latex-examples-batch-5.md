# LaTeX-examples Batch 5 Visual QA

Acceptance target: every case must render in TikZKit, have a usable tikztosvg reference when the source can compile locally, and match the reference in structure, coordinates, dimensions, text, strokes, colors, arrows, and layers. Pixel-diff numbers are supporting evidence only.

| Case | Current visual status | Observed difference | Shared owner / next action |
| --- | --- | --- | --- |
| real-world-decorations-text-alignment | Near pass | TikZKit and tikztosvg both place the left-aligned words 8mm after the first guide endpoint, place the right-aligned words 8mm before the second endpoint and below its guide, center and rotate the curved words along the dashed Bezier, and stretch the spaces across the fitted baseline. The sheet has only sparse glyph/grid raster residuals; no text is missing, clamped to the midpoint, or shifted to the wrong side of its path. | Read local pgflibrarydecorations.text.code.tex and pgfmanual-en-library-decorations.tex: native text decoration advances a glyph box at a time, computes left/right/center starts after indents, and allows signed normal raise. TikZKit now retains alignment/indent/fit metadata in the IR and applies it in the glyph renderer. Artifacts: test/fixtures/implementation-examples/output/{tikzkit-grid-png,tikztosvg-grid-png,diff}/real-world-decorations-text-alignment*. |
| real-world-intersections-alias-coordinate-style | Near pass | TikZKit and tikztosvg both render the same purple-outlined pale-yellow circular coordinate marker at the red/blue intersection, with math `C` above it. The marker center, diameter, fill, stroke, label direction, and grid-relative location align; residual red pixels in the sheet are glyph outlines and thin grid antialiasing. | The native `by` value is consumed by `\foreach`, so comma-separated node keys must first be placed in a style and used as `by={[intersection mark]C}`. `materializeIntersections()` now routes box-sized alias coordinates through the shared `createNode()` path with an absolute intersection point, preventing scope transforms from being applied twice. Artifacts: `test/fixtures/implementation-examples/output/{tikzkit-grid-png,tikztosvg-grid-png,diff}/real-world-intersections-alias-coordinate-style*`. |
| real-world-intersections-alias-label | Near pass | Before the fix, `name intersections={...,by={[label={[fill=white,inner sep=1pt]above:$C$}]C}}` registered coordinate `C` but omitted its label entirely. After the fix, TikZKit and tikztosvg both visibly place the math-italic `C` above the black intersection marker on the same blue/red crossing; the white label box clears the two strokes. The remaining pixels are expected Computer Modern glyph and thin-grid rasterization, not a missing label or coordinate shift. | Read local `tikzlibraryintersections.code.tex`, `pgflibraryintersections.code.tex`, and `pgfmanual-en-tutorial-Euclid.tex`: `by` consumes `[coordinate options]name` and invokes a native coordinate. TikZKit now parses each alias into `{name,options}` and sends `label=...` through the shared coordinate-label renderer. Artifacts: `test/fixtures/implementation-examples/output/{tikzkit-grid-png,tikztosvg-grid-png,diff}/real-world-intersections-alias-label*`. Arbitrary alias coordinate shapes/drawing options remain partial. |
| real-world-intersections-sorted-aliases | Near pass | Before the fix, `by={[label=below:left]left,[label=below:right]right}` left both visible marker nodes at the origin because the optional coordinate options became part of the coordinate name. The red named path is intentionally drawn right-to-left, so only `sort by=route` can correctly put the orange `left` and green `right` markers on the left and right intersections. After the fix, TikZKit and tikztosvg share the same 190x114px canvas; both markers, labels, blue route, red baseline, and grid are visibly aligned. The remaining 330 changed pixels (1.52%) are thin-grid and glyph rasterization, not coordinate or order error. | Read local `tikzlibraryintersections.code.tex` and `pgflibraryintersections.code.tex`: PGF stores solution times for both paths and sorts on the requested path. TikZKit now retains flattened-path traversal times, honors `sort by` for either named path, and strips optional alias coordinate options before registering the alias. Artifacts: `test/fixtures/implementation-examples/output/{tikzkit-grid-png,tikztosvg-grid-png,diff}/real-world-intersections-sorted-aliases*`. |
| feed-forward-perceptron | Near pass | Topology, coordinates, sizes, and colors match; small Latex-tip and circle-boundary differences remain. | Arrow tip geometry and endpoint shortening. |
| b-tree-node | Near pass | TikZKit lowers the unpositioned inline `\tikz \node` into a real 13-part `rectangle split` node. All `A.one` through `A.thirteen` anchors, six pointer arrows, per-part fills, centered digits, and the `isLeaf` / `n` / key labels render with zero diagnostics. Preserving the inherited `\tt` family during box measurement aligns the inner node at 191.47pt x 13.11pt (tikztosvg: about 190.93pt x 13.00pt) and the yellow outer box at 204.19pt x 20.58pt (tikztosvg: about 203.60pt x 20.43pt). Remaining pixels are glyph outlines and a small outer-canvas variance. | `src/engine/evaluate.js` text-engine font forwarding and split layout; local `pgflibraryshapes.multipart.code.tex` reviewed. Artifacts: `output/{tikzkit-grid-png,tikztosvg-grid-png,diff}/latex-examples-b-tree-node*`. |
| b-tree-2-small-2 | Near pass | The same shared multipart-font rule keeps the root and all three leaf nodes at the native cell positions. Empty slots, centered `7/11`, `1/4`, `4/8`, `14/15/16`, per-part colors, and the three pointer arrows agree on a 384x142px reference canvas; TikZKit is only 1px wider. Remaining red pixels are the rasterized equal-width glyph outlines. | Cross-case verification for `rectangle split`, inherited `font=\tt`, and named-part anchors; artifacts: `output/{tikzkit-grid-png,tikztosvg-grid-png,diff}/latex-examples-b-tree-2-small-2*`. |
| flowchart-network-design | Near pass | TikZKit and tikztosvg now share the same 309x583px canvas, rounded block and diamond dimensions, wrapped node text, all straight/orthogonal return paths, and both `no` labels. The `decide2 -| update` label and `update |- init` loop are visibly at their native positions; residual pixels are only Computer Modern glyph outlines and thin-stroke antialiasing. | Reviewed local `tikz.code.tex` node `text width` storage and `pos` aliases; artifacts: `output/{tikzkit-grid-png,tikztosvg-grid-png,diff}/latex-examples-flowchart-network-design*`. |
| force-distance-diagram-constant | Near pass | TikZKit and tikztosvg now agree on the blue constant `F=3`, the translucent green soft-clipped area from `x=1` to `x=3`, and positional tick labels `$a$`, `$b$`, `$c$`. Empty entries in `xticklabels` / `yticklabels` retain their tick slots, so neither label shifts. The remaining difference is only comparison-grid dash rasterization and a slight outer-canvas variance. | PGFPlots positional tick-label list lowering in `src/pgfplots/ticks.js`; local `pgfplots.code.tex` reviewed. |
| force-distance-diagram | Near pass | TikZKit and tikztosvg now both render the blue `sqrt(x)` curve, the green `fill between` region softly clipped from `a=1` to `b=3`, the middle axes, and `x \coloneqq s` / `y \coloneqq F` labels with no diagnostics. The canvas widths differ by about 0.8pt and comparison-grid dashes rasterize differently; no source geometry is missing. | `src/pgfplots/fillBetween.js`; local PGFPlots fillbetween source reviewed. |
| 3d-function-semicubical-parabola | Improved, pending | Before the fix, `tikzpicture[scale=0.5]` entered the inherited PGFPlots defaults and then the outer TikZ transform, so the JS 3D surface was visibly quarter-sized at about `56.65pt x 42.84pt`. It now measures `92.84pt x 66.51pt`, close to the `89.92pt x 61.30pt` tikztosvg reference: the orange semicubical surface, 3D box, x/y/z labels, and left colorbar occupy the same intended scale. The generated MacTeX PNG and tikztosvg reference agree on that structure. Remaining visual differences are surface sampling/rasterization, curve antialiasing, and a small outer-bbox excess, not an omitted axis or a second transform. | Reviewed `/usr/local/texlive/2025/texmf-dist/tex/generic/pgfplots/pgfplots.code.tex`: `/pgfplots/scale` writes x/y/z post scales and is distinct from the enclosing TikZ picture transform. TikZKit now filters picture transform keys before inheriting PGFPlots defaults. Artifacts: `test/fixtures/examples/output/{native-png,tikzkit-grid-png,tikztosvg-grid-png,diff}/latex-examples-3d-function-semicubical-parabola*`; tikztosvg SVG: `test/fixtures/examples/output/tikztosvg-svg/latex-examples-3d-function-semicubical-parabola.svg`. |
| ellipsoid | Improved, pending | The `addplot3[surf]` ellipsoid now uses the native tight inferred `-5..5` x/y domain rather than the generic 2D 10% enlargement. Its green-blue parabolic mesh, open x/y/z axes, ticks, and labels visibly align with both the MacTeX PNG and tikztosvg. The changed-pixel ratio fell from 48.23% before the shared range fix to 25.89% after the range and `zlabel` placement fixes. TikZKit is now 319x238px versus tikztosvg 321x241px; the remaining difference is a small outer-canvas height/bbox shift and glyph/mesh antialiasing, so this is not yet a visual pass. | Read `/usr/local/texlive/2025/texmf-dist/tex/generic/pgfplots/pgfplots.code.tex`: every non-boxed x/y/z axis disables automatic enlargement, while every 3d description puts the z label at `ticklabel cs:0.5` with `near ticklabel`. Shared implementation: `src/pgfplots/rangeResolver.js` and `src/pgfplots/axis3d.js`; artifacts: `test/fixtures/examples/output/{native-png,tikzkit-grid-png,tikztosvg-grid-png,diff}/latex-examples-ellipsoid*`, plus `test/fixtures/examples/output/tikztosvg-svg/latex-examples-ellipsoid.svg`. |
| geometry-1 | Improved, pending | Negative `-90` labels are now below their points; small font/baseline differences remain. | Fixed core numeric label-angle parsing; continue math text metrics. |
| geometry-2 | Improved, pending | Negative `-90` labels are now below their points; phi-label baseline and bbox still differ. | Fixed core numeric label-angle parsing; continue math text metrics. |
| geometry-3 | Near pass | The legacy `\usetkzobj` source now renders its computed `C` line intersection, all four line/segment constructions, five black points, and `A/B/C/P/Q` labels. TikZKit is 213x112px versus tikztosvg 213x113px; remaining red pixels are glyph antialiasing and the 1px bbox difference, not blank output or lost geometry. | `tkzDefPoints`, `tkzInterLL`, `tkzGetPoint`, extended `tkzDrawLine`, and `tkzDrawSegments`; artifacts in `output/*grid-png/latex-examples-geometry-3.png`. |
| geometry-4 | Near pass | TikZKit and tikztosvg render the same three solid triangle edges, two dashed constructions, extended baseline, four black points, and `P/Q/A/B` labels on an identical 213x150px canvas. The diff is distributed across otherwise coincident thin strokes and math glyph edges, which is SVG rasterization rather than lost tkz-euclide geometry. | Reviewed `tkz-draw-eu-lines.tex` and `tkz-draw-eu-points.tex`; `tkzDefPoints`, `tkzDrawSegments`, extended `tkzDrawLine`, point markers, and labels. Artifacts: `output/{tikzkit-grid-png,tikztosvg-grid-png,diff}/latex-examples-geometry-4*`. |
| geometry-5 | Near pass | The computed `C` intersection, four extended construction lines, six black points, and all six labels agree with tikztosvg on the same 266x161px canvas. Remaining pixels are limited to Computer Modern label outlines. | Reviewed `tkz-tools-eu-intersections.tex`; `tkzInterLL` / `tkzGetPoint` expansion and shared line/point/label lowering. Artifacts: `output/{tikzkit-grid-png,tikztosvg-grid-png,diff}/latex-examples-geometry-5*`. |
| geometry-6 | Visual pass | TikZKit and tikztosvg agree on the two green angle arcs, all three labeled lines, both point markers, and the two `||` equal-segment marks. The diff is now confined to glyph antialiasing around `P/Q/f/g/h`; no geometry or marker is missing. | `tkzMarkSegments` now expands `|`, `||`, and `|||` into native tangent-offset bar geometry. Local `tkz-draw-eu-lines.tex` and `tkz-lib-eu-marks.tex` reviewed. |
| geometry-7 | Near pass | The red 120-degree sector, green angle arc, three extended triangle edges, three point markers, and `alpha/beta/P/Q/R` labels all render. `arc[start angle=0,delta angle=-120,radius=.4]` now terminates after two cubic segments instead of incorrectly becoming a full circle; the changed area fell from 4.79% to 2.03%. Remaining difference is a 1px width/bbox variance and glyph antialiasing. | Shared `buildArc` support for `delta angle` and explicit `end angle=0`; `tkzMarkAngle` plus ordinary TikZ relative coordinates. Artifacts: `output/{tikzkit-grid-png,tikztosvg-grid-png,diff}/latex-examples-geometry-7*`. |
| geometry-8 | Near pass | TikZKit and tikztosvg now agree on the five points, `M` intersection, five structural segments, four angle labels, three green parallel arcs, and the red/blue/green single arcs. TikZKit raster is 272x114px versus tikztosvg 269x113px; the remaining diff is a global bbox/text-raster offset rather than missing geometry. | Local `tkz-euclide` 5.11c angle-label and multi-arc behavior verified against the generated reference. |
| geometry-9 | Near pass | Both renderers now show the five points, five structural lines, two pale-green triangles, the red single angle arc, green triple angle arc, and all five labels. TikZKit is 85.42pt wide versus tikztosvg 85.89pt; remaining difference is text-outline/bbox rasterization. | `tkz-euclide` multi-arc expansion plus disposable tikztosvg legacy-size normalization. |
| elevation-chart | Near pass | The CSV profile, smooth curve, labels, and plot dimensions match the tikztosvg reference. The raw PNG diff is mainly the different outer bbox and text rasterization. | Plot-level `smooth` overrides an axis-level `ybar`; preserve this precedence while extending histogram handlers. |
| earth-skizze-2 | Near pass | `scale=3.5` now composes correctly with `scope[shift={(DEnd)}]`: the gray `25°` sector is attached to the diagonal endpoint instead of being pushed to the far upper-right, so both renderers share the same 319px width. The two hemispheres, dashed latitude, green segment, centered `6370 km` path text, and double-stealth measurement arrow are all visible and aligned. The remaining difference is a 1px output-height variance plus normal glyph antialiasing around the small place names. | Core scope-transform composition in `src/engine/evaluate.js`; artifacts: `output/tikzkit-grid-png/latex-examples-earth-skizze-2.png`, `output/tikztosvg-grid-png/latex-examples-earth-skizze-2.png`, and `output/diff/latex-examples-earth-skizze-2-sheet.png`. |
| coordinate-system-1 | Near pass | Defined points, the two extended `tkzDrawLine` axes, the green right-angle mark, point `P`, and `g_1/g_2/X` labels agree with tikztosvg. The remaining difference is a 2px output-height variance and normal math-glyph rasterization. | `tkzDefPoints`, `tkzDrawLine`, `tkzMarkAngle`, and `tkzLabelLine`; artifacts in `output/*grid-png/latex-examples-coordinate-system-1.png`. |
| coordinate-system-2 | Near pass | The orthogonal constructions through `P`, both `tkzInterLL` projections, braces, quadrilateral, labels, and three point markers all render at the reference coordinates. Residuals are a uniform 1px raster shift and text outlines, not missing construction geometry. | `tkzDefLine[orthogonal=through]`, `tkzInterLL`, and `tkzDrawPolygon`; artifacts in `output/*grid-png/latex-examples-coordinate-system-2.png`. |
| coordinate-system-3 | Near pass | The extended axes, two orthogonal projections, `R` intersection, black quadrilaterals, and green/orange segment pairs agree structurally with tikztosvg. Output differs by 1px width/height plus label antialiasing only. | Same construction slice; artifacts in `output/*grid-png/latex-examples-coordinate-system-3.png`. |
| equilateral-triangle | Near pass | TikZKit and tikztosvg agree on the scaled equilateral geometry, three green 60-degree sectors, calc midpoints, and all `A/B/C`, `a/b/c`, `\\alpha/\\beta/\\gamma` labels. TikZKit's canvas is 119.02pt by 108pt versus tikztosvg's 119.92pt by 103.34pt; the remaining mismatch is only global math-text/bbox padding. | `src/engine/evaluate.js` coordinate/calc/scoped-transform lowering; local `tikzlibrarycalc.code.tex` and `tikz.code.tex` reviewed. |
| interiour-exteriour-angles-triangle | Near pass | TikZKit and tikztosvg agree on the three extended triangle edges, all three black vertex points, green interior angle arcs, and six blue exterior triple-arc marks. The native 0.4pt PGF offset makes the triple arcs visibly distinct while retaining native `fill=none` behavior despite `fill=blue!20` in the source. Remaining difference is limited to `P/Q/R` glyph rasterization and a 1px canvas-height variance (138x130px vs 138x131px), with no missing geometry. | Local `tkz-draw-eu-angles.tex` and `tkz-tools-eu-angles.tex` reviewed; artifacts: `output/tikzkit-grid-png/latex-examples-interiour-exteriour-angles-triangle.png`, `output/tikztosvg-grid-png/latex-examples-interiour-exteriour-angles-triangle.png`, `output/diff/latex-examples-interiour-exteriour-angles-triangle-sheet.png`. |
| inversion-am-kreis | Near pass | Before this slice TikZKit lost `tkzTangent`, both named tangent points, the `dZ` intersection, the angle mark, all dependent dashed lines, and the orange semicircle. It now agrees with the local native and tikztosvg reference on the external point, radius-one semicircle, two tangent constructions, inversion point, axes, and both mathematical labels. Remaining visual difference is limited to Computer Modern glyph rasterization and a 1px canvas extent difference. | Reviewed TeX Live 2025 `tkz-euclide/tkz-obj-eu-lines.tex`: `tkzDefTangent[from with R=P](O,r)` intersects the radius circle with the midpoint circle, then orders `tkzFirstPointResult` by the directed angle at the tangent. Also reviewed `tkz-base/tkz-obj-axes.tex` for the legacy `tkzAxeXY` reference. Artifacts: `output/native-png/latex-examples-inversion-am-kreis.png`, `output/tikzkit-grid-png/latex-examples-inversion-am-kreis.png`, `output/tikztosvg-grid-png/latex-examples-inversion-am-kreis.png`, `output/diff/latex-examples-inversion-am-kreis-sheet.png`. |
| hyperbolic-triangle-exterior-angles | Near pass | Before this slice, the three white orthogonal-circle fills were interpreted as black and hid the construction. TikZKit now visibly matches the tikztosvg reference: the white Poincare disk, its light-gray `0.2pt` boundary, three colored geodesic arcs, three red points, and `A/B/C/alpha/beta/gamma` labels all occupy the same grid-relative positions. Both output canvases are 170.28pt (reference 170.27pt); the remaining diff is only subpixel arc/Computer Modern glyph rasterization. | Read local `tkz-draw-eu-circles.tex`, `tkz-obj-eu-circles.tex`, `tkz-draw-eu-angles.tex`, `tkz-draw-eu-points.tex`, and `tkz-euclide.cfg`. `tkzDrawCircle` applies `circle style` before user options, `orthogonal through` uses point inversion plus a circumcenter, and `tkzClipCircle` is a real disk clip. Artifacts: `test/fixtures/examples/output/{tikzkit-grid-png,tikztosvg-grid-png,diff}/latex-examples-hyperbolic-triangle-exterior-angles*`; tikztosvg input/reference: `test/fixtures/examples/output/{tikztosvg-input,tikztosvg-svg}/latex-examples-hyperbolic-triangle-exterior-angles.*`. |
| hyperbolic-triangle-interior-angles | Near pass | The same circle construction now renders with the source's sans-serif math setting: white disk, light-gray boundary, green/blue/red geodesics, red vertices, and directional labels all match the local reference. The panel shows no clipped-away geometry or wrong fill; residual pixels are the expected SVG text/curve antialiasing around `A/B/C` and Greek glyphs. | Shared verification for `tkzDrawCircle`, `tkzClipCircle`, `tkzMarkAngles`, `tkzLabelPoints`, scoped `scale=3`, and `font=\\sffamily\\sansmath`. Artifacts: `test/fixtures/examples/output/{tikzkit-grid-png,tikztosvg-grid-png,diff}/latex-examples-hyperbolic-triangle-interior-angles*`; tikztosvg input/reference: `test/fixtures/examples/output/{tikztosvg-input,tikztosvg-svg}/latex-examples-hyperbolic-triangle-interior-angles.*`. |
| real-world-angle-pic-reflex-and-right | Near pass | TikZKit now renders both standard `pics/angle` and `pics/right angle`: the 8mm blue right-angle square, red filled 270-degree counterclockwise reflex sector, and both quote labels visually agree with tikztosvg. The render is 81x74px versus the reference 84x76px; remaining red pixels are the outer bbox and Computer Modern glyph rasterization, not missing pic geometry. | Reviewed local `tikzlibraryangles.code.tex`; shared `createAnglePic` / inline `buildAnglePic` now inherit picture-level radius/eccentricity/text, use the native counterclockwise sweep, and produce cubic arc segments. Artifacts: `test/fixtures/implementation-examples/output/{tikzkit-grid-png,tikztosvg-grid-png,diff}/real-world-angle-pic-reflex-and-right*`. |
| hyperbolische-geometrie-axiom-1-2 | Near pass | TikZKit now renders the `tkzInit` Cartesian axes with ticks, dashed construction triangle and perpendicular, point labels, and the orange radius arc. The source geometry is present in both renderers; the remaining 264x200px vs 261x195px extent difference is driven by `tkzAxeXY` tick/label metrics and its resulting bbox, not by a missing axis or construction command. | `tkz-base/tkz-obj-axes.tex`, `tkz-euclide/tkz-obj-eu-lines.tex`, and `tkz-tools-eu-intersections.tex` reviewed; artifacts: `output/tikzkit-grid-png/latex-examples-hyperbolische-geometrie-axiom-1-2.png`, `output/tikztosvg-grid-png/latex-examples-hyperbolische-geometrie-axiom-1-2.png`, `output/diff/latex-examples-hyperbolische-geometrie-axiom-1-2-sheet.png`. |
| graph-banner | Near pass | Structure and node sizes match; narrow stroke-edge differences remain. | Line width, clipping, and raster alignment. |
| graph-circles | Improved, pending | The native fixed-pitch snake now keeps PGF's startup crest and alternating cosine/sine waves instead of stretching a sampled sine to each edge. Long red, purple, and black edges visibly match the reference wave order and pitch more closely; both canvases are 286x377px. Snake state now continues through each multi-segment decorated subpath, so the prior corner restart and repeated pre/post lengths are gone. Remaining difference: the renderer flattens curve geometry and uses approximate normal transitions at sharp corners, leaving thin red residuals despite matching phase continuity. | `src/engine/evaluate.js` native snake state machine and `applySnakeDecorationToSubpaths`; local `pgflibrarydecorations.pathmorphing.code.tex`, `tikzlibrarydecorations.code.tex`, and `pgfmoduledecorations.code.tex` reviewed. Artifacts: `outputs/qa-snake-graph-circles-after/{tikzkit-grid-png,tikztosvg-grid-png,diff}/latex-examples-graph-circles*`. |
| graph-content-and-structure | Near pass | Node topology, two bent edges, dashed group boxes, and both zero-distance labels now align. The TikZKit height is 121.98pt versus tikztosvg 122.09pt; only text rasterization/glyph-outline variance remains. | Label/pin nodes now include their TeX inner-sep layout box in the SVG bbox. |
| graph-mrf-image-segmentation | Near pass | TikZKit now uses compact scriptstyle metrics for single-letter formulas such as `$x_1$` and `$y_9$` inside `minimum size=10pt` circles. The red/blue node diameters contract from about 14.4pt to 12.2pt, and the output canvas contracts from 213x107px to 210x104px (tikztosvg: 209x103px). The colored node lattice, every edge, and every center now agree; remaining pixels are math-glyph and subscript-baseline rasterization. | Shared simple-script TeX box metric in `src/tikz/textMetrics.js`; artifacts: `output/{tikzkit-grid-png,tikztosvg-grid-png,diff}/latex-examples-graph-mrf-image-segmentation*`. |
| graph-triangles | Visual pass | TikZKit and tikztosvg now agree on 66 red nodes and 165 edges; the visual diff has only 62 antialiasing pixels (0.0205%). | Runtime `\ifthenelse` numeric relations and TeX-compatible deferred `\breakforeach`; the reference runner now forwards `ifthen`. |
| graph-v6-e8 | Near pass | Nodes/colors match; subtle clipping and line-cap differences remain. | Compound path caps and node-boundary clipping. |
| halbleiterspeicher-klassifizierung | Near pass | TikZKit and tikztosvg both render the 11 gray nodes, all parent-child edges, and the same tree hierarchy. Inspection of the SVG scene confirms that generated child edges inherit the picture `thick` line width; the 4px output-width difference is driven by CMU Sans versus the native TeX text metrics in the long right-hand labels, not missing edge styling. | Reviewed local `tikzlibrarytrees.code.tex`; generated tree edges use picture options through `createNodeTreeChildren` / `addTreeEdge`. Artifacts: `output/{tikzkit-grid-png,tikztosvg-grid-png,diff}/latex-examples-halbleiterspeicher-klassifizierung*`. |
| half-space | Near pass | Browser JS and tikztosvg now agree on both wide hatch regions, their red/green overlap, axes, arrows, and labels. Raw PNGs retain a 2px height difference and font/hatch antialiasing variance. | `\\pgfdeclarepatternformonly` is parsed and rendered as SVG pattern geometry; remaining mismatch is raster-level. |
| hard-limit-function | Near pass | Step function matches; extra zero tick and bbox padding remain. | Middle-axis tick suppression and bbox. |
| haskell-type-classes | Improved, pending | `\tikzstyle` inheritance, the 3x6 matrix of named ellipse nodes, multiline text, and all twelve directed edges now render as the same class hierarchy rather than wide rectangles. Remaining differences are native TeX wrapping in the long `Enum`/`Bounded` cells, row/column natural sizes, and therefore ellipse/arrow endpoint positions. | Matrix natural-size and `text width=3cm` ellipse metrics; local `tikzlibrarymatrix.code.tex` and PGF ellipse shape rules reviewed. |
| heap | Near pass | The heap's 15 circular nodes and 14 child edges have matching topology, coordinates, and `very thick` black stroke semantics. The reference uses a 1.19553pt stroke before its output transform; TikZKit carries the same `very thick` style through generated child edges. Residual pixels are the Computer Modern number glyphs and circle-boundary antialiasing, not a lost tree edge style. | Reviewed local `tikzlibrarytrees.code.tex`; child-edge picture-style inheritance is covered by `test/interpreter.test.js`. Artifacts: `output/{tikzkit-grid-png,tikztosvg-grid-png,diff}/latex-examples-heap*`. |
| hidden-markov-model-abc-2 | Near pass | Both renderers now show all three gray circular states, compact `\nicefrac` emission rows, state labels, transition labels, and self loops. The browser output is 249x123px versus the 247x119px reference: residual differences are circle text metrics, loop-arrow placement, and label rasterization, not a missing `units` macro or unavailable reference. | Local `units.sty` reviewed; `units` loads `nicefrac` and only configures `tight`/`loose` spacing. Browser `\nicefrac` lowering is in text normalization and SVG fallback. Artifacts: `output/{tikzkit-grid-png,tikztosvg-grid-png,diff}/latex-examples-hidden-markov-model-abc-2*`. |
| hidden-markov-model-abc | Near pass | The two circular probability states, three compact fractional rows in each state, `9/10` transition, and two self loops now render in both outputs. Remaining difference is the same small circle/baseline and loop-arrow metric variance (152x100px JS versus 150x97px reference), not unsupported `\nicefrac`. | Same `units`/`nicefrac` text path; artifacts: `output/{tikzkit-grid-png,tikztosvg-grid-png,diff}/latex-examples-hidden-markov-model-abc*`. |
| hidden-markov-model-three-state-symbol | Near pass | Layout is close; loop arrows, endpoints, and lower labels are slightly shifted. | Loop arrow geometry, font baseline, bbox. |
| histogram-simple | Improved, pending | `hist={bins=3}` now derives the native bin edges and counts, and `ybar interval` renders the three filled bars at heights 3, 2, and 7 with `1--4`, `4--7`, and `7--10` interval labels. Remaining difference is plot-area/bbox sizing and tick/grid placement, not missing bins. | `src/pgfplots/histogram.js`, `bars.js`, and interval tick-label lowering; local `pgfplotsplothandlers.code.tex` reviewed. |
| histogram-large-1d-dataset | Near pass | `ybar interval` bars, white foreground grid, seven rotated `nodes near coords` labels, interval ticks, and both axis labels now align with the tikztosvg layout. Local TeX measurements of `current axis.outer south west/north/east` corrected the crop from 344.55pt x 139.74pt to 339.15pt x 133.87pt (reference: 338.98pt x 133.24pt); rendered canvases are 453x179px and 452x178px. Remaining differences are Computer Modern glyph outlines and thin-line antialiasing, not missing plot semantics. | Reviewed `pgfcorescopes.code.tex`, `tikz.code.tex`, and `pgfplots.code.tex`; updated `src/pgfplots/axisTikzLowering.js`, `plotNodes.js`, and `labels.js`; artifacts: `test/fixtures/examples/output/{tikzkit-grid-png,tikztosvg-grid-png,diff}/latex-examples-histogram-large-1d-dataset*`. |

## Shared fixes completed in this batch

- PGFPlots now keeps the primary data plot box for an immediately following matching-size secondary `hide x axis, axis y line*=right` overlay, while preserving the secondary axis's own TeX-style layout allocation for the final picture bounds. In `csv-line-plot-two-axes`, the blue/red primary series and orange/black right-axis series now share the same top and bottom frame; the right tick labels occupy the matching side without a duplicate x axis. The local `pgfplots.code.tex` state-machine implementation for `hide x axis` and `axis y line*=right` was reviewed. QA artifacts: `outputs/qa-pgfplots-overlay-right-axis-after/{native-mactex,tikzkit-grid-png,tikztosvg-grid-png,diff}/latex-examples-csv-line-plot-two-axes*`. The comparison improved from 18.12% to 17.49% changed pixels; a 10pt by 5pt outer-canvas/text calibration difference remains.

- Named-path intersections retain a traversal time for each flattened source path. `name intersections={...,sort by=<first-or-second-path>,by={...}}` sorts aliases by the requested path and consumes optional alias coordinate options before registering the name; this preserves the native `left/right` association on multi-intersection paths.
- Named-intersection aliases now preserve their coordinate option payload. The common `by={[label=above:C]C}` form is passed through the shared coordinate-label layout path, while a declared node style such as `by={[intersection mark]C}` uses the normal coordinate/node renderer; both participate in SVG bounds rather than being silently discarded.
- Numeric label and pin directions now preserve unary minus, so `-90` and `-45` no longer become positive angles.
- Example resources can be declared in the manifest, loaded by the Node renderer and browser workbench, and rewritten to stable fixture-relative paths for tikztosvg.
- `birthday-paradox` now declares and carries its original tab-separated `data.csv`. Its browser plot once contained only the analytic red approximation and blue guide lines; it now also renders the 61-point blue probability series, with no table-resolution diagnostic. QA artifacts: `outputs/qa-birthday-paradox-after/{native-mactex,tikzkit-grid-png,tikztosvg-grid-png,diff}/latex-examples-birthday-paradox*`.
- `circular-sector-centroid` accepts a node name and node options in either native post-`at` order: `at (...) [options] (name)` or `at (...) (name) [options]`. This restores the visible top `b` label and the later `-- (b)` vertical radius; MacTeX, TikZKit, and tikztosvg all show the annular sector, two arc arrows, centroid construction, and dimension marks. QA artifacts: `outputs/qa-node-at-options-after/{native-mactex,tikzkit-grid-png,tikztosvg-grid-png,diff}/latex-examples-circular-sector-centroid*`.
- Standalone PGFPlots custom legends now lower the native `pgfplots@init@cleared@structures` / `pgfplots@addlegendimage` / `pgfplots@createlegend` recipe into ordinary TikZ paths and `\footnotesize` nodes. `informatikstudium-kit-abhaengigkeitsgraph` now visibly includes its centered six-row legend: four module-color samples and black/red dependency arrows. MacTeX, TikZKit, and tikztosvg agree on its absolute lower-left placement and sample ordering; the large graph's pre-existing vertical spacing remains a separate partial issue. QA artifacts: `outputs/qa-customlegend-final/{native-mactex,tikztosvg-grid-png,tikzkit-grid-png,diff}/latex-examples-informatikstudium-kit-abhaengigkeitsgraph*`.
- `elevation-chart` now carries its source `data.csv`; both TikZKit and tikztosvg can consume it.
- Plot-level line handlers such as `smooth` override an axis-level `ybar` default, so the elevation profile is rendered as one smooth `axis-plot`, not a bar series.
- The `bias-variance` legend uses native left-aligned cells and has been visually verified in the browser.
- Form-only PGF pattern declarations now retain their tile size, line width, and move/line geometry through the parser, interpreter, and SVG `<pattern>` renderer; `half-space` no longer loses its named coordinates or hatch fills.
- `\ifthenelse` is now parsed and evaluated after loop-local PGF math changes. `\breakforeach` completes the current iteration and then exits only its innermost loop, matching the `pgffor` behavior exercised by `graph-triangles`.
- Labels and pins retain their unpainted node layout box for SVG bounds, matching TikZ's `every label` / `every pin` behavior even when `draw=none,fill=none`; this removes the graph-content-and-structure viewport shift.
- `tkz-euclide` now expands unmarked `\tkzMarkAngle` variants `l`, `ll`, and `lll`. The multi-arc radii follow the local implementation: center radius with additional radii at `size cm ± 2.5\pgflinewidth`. The reference-input normalizer converts legacy unit-bearing `ll`/`lll` sizes (for example `0.4cm`) to the numeric centimeters expected by the current local macro, avoiding its `0.4cmcm` error without changing user sources.
- `tkzMarkSegment` and `tkzMarkSegments` now render bar marks `|`, `||`, and `|||` at the requested path position. The bars use the same perpendicular geometry and `2\pgflinewidth` / `3\pgflinewidth` tangent offsets as the local `tkz-euclide` plot-mark declarations.
- `angles` now follows `tikzlibraryangles.code.tex`: an `angle` pic always sweeps counterclockwise (including reflex sectors), while `right angle` uses the native three-side square. Both direct `\pic` and inline path pics inherit picture-level `angle radius`, `angle eccentricity`, and `text` settings.
- The `snake` decoration now follows PGF's state machine: a fixed `.3125`-segment startup crest, then native `\pgfpathcosine` / `\pgfpathsine` cubic coefficients, an end-down/end-up state, and a final straight remainder. This retains requested `pre length` / `post length` rather than resampling waves across the full source segment.
- PGFPlots `fillbetween` now lowers named 2D function or coordinate paths into a closed, pre-main SVG area. `soft clip={domain=a:b}` clips both paths at the mathematical interval boundaries before the polygon is closed; `fill` and `fill opacity` are retained. The reference runner also forwards the source `mathtools` package so `\coloneqq` is available to local `tikztosvg`.
- Inferred 3D `surf` domains no longer inherit 2D 10% x/y enlargement. PGFPlots' non-boxed 3D defaults keep those ranges tight, and its default z-axis label follows `ticklabel cs:0.5` / `near ticklabel`; TikZKit now uses the corresponding one-centimetre minimum normal offset.
- Explicit PGFPlots `xticklabels` and `yticklabels` now retain empty comma-delimited entries as positional placeholders, matching local `\pgfplotslistnew` behavior. This keeps later labels attached to their intended ticks.
- Rectangle-split layout forwards the fully resolved TeX font family to the shared text engine. This makes a picture-level `font=\tt` affect both the SVG glyphs and the measured cell widths, matching PGF's `cmtt10` split-node geometry.
- `tkzClipCircle` is now retained as a renderer-level circular clip, rather than merely using white overdraw. Clipped paths get SVG `<clipPath><circle>` output and their bounds are intersected with the same disk. This keeps the rendered image and SVG viewBox consistent with local `tkz-euclide`; the shared TikZ `color` rule also now supplies a bare `fill` with the current color while explicit `fill=<color>` continues to win.

- `lindenmayersystems` now lowers the deterministic PGF rule-expansion model to ordinary renderer-neutral TikZ paths. The real PGF-manual Koch fixture uses `\pgfdeclarelindenmayersystem`, `F -> F-F++F-F`, `l-system`, `step`, `angle`, `axiom`, and `order`; before this change the JS panel was empty, while the repaired JS, tikztosvg, and native MacTeX panels all show the same complete blue snowflake. The after-diff is blank (`0/1` different): `outputs/qa-lindenmayer-after/{native-mactex,tikzkit-grid-png,tikztosvg-grid-png,diff}/koch-snowflake*`. Read `/usr/local/texlive/2025/texmf-dist/tex/generic/pgf/libraries/pgflibrarylindenmayersystems.code.tex`, `.../frontendlayer/tikz/libraries/tikzlibrarylindenmayersystems.code.tex`, and `.../doc/generic/pgf/pgfmanual-en-library-lsystems.tex`. Supported turtle symbols are `F/f/+/-/[ ]`; randomized step/angle, arbitrary TeX symbol bodies, global defaults, and anchor-node placement remain partial.

- `arrows` now lowers supported `\pgfarrowsdeclare` programs into ordinary inline SVG arrow-tip geometry. The real declared `leaf` driver starts with a missing green/red endpoint shape in the JS panel, while tikztosvg and MacTeX show leaf tips. After the repair, TikZKit, tikztosvg, and MacTeX all visibly render the filled leaf at the green path end and the red path start; its local SVG cubics match the reference orientation and extent. A regression also covers a braced multiword declaration (`-{kite tip}`) with move/line/cubic commands and `qfillstroke`. The JS/tikztosvg PNG difference improved from 798 changed pixels (17.35%) to 573 (12.46%); the remaining difference is a roughly 1pt outer-canvas variance and thin-stroke rasterization, not missing arrow geometry. Artifacts: `outputs/qa-arrows-declared-after-v3/{native-mactex,tikzkit-grid-png,tikztosvg-grid-png,diff}/arrows-declared-leaf-tip*`. Read `/usr/local/texlive/2025/texmf-dist/tex/generic/pgf/libraries/pgflibraryarrows.code.tex`, `.../frontendlayer/tikz/libraries/tikzlibraryarrows.code.tex`, and `.../doc/generic/pgf/pgfmanual-en-{base,library,tikz}-arrows.tex`. Supported declaration bodies use constant `\pgfpoint` move/line/cubic/arc paths with `qfill`, `qstroke`, or `qfillstroke`; TeX arithmetic in setup code, clipping, arbitrary macros, and composited/multiple tips remain partial.

- decorations.text now preserves native text-align starts (left, right, center), left/right indents, fit-to-path gap stretching, and signed raise through the renderer's existing per-glyph tangent sampler. Text effects, character-specific styling, replacement, grouping, repetition, and exact TeX glyph boxes remain intentionally partial.

- circuitikz basic bipoles now render the American independent voltage source as a circle with internal + / - symbols instead of the European center bar. The real basic-bipoles mesh verifies to[R=...], to[C=...], to[V<=...], V< direction, [american], and package options [siunitx,RPvoltages]: the resistor zigzag, capacitor plates, source polarity, external reference-polarity arrow, component labels, and wire loop all appear in both panels. The QA runner now preserves supported \usepackage[...] options for tikztosvg through a temporary local .sty wrapper, and normalizes the circuitikz environment to its documented tikzpicture alias so the reference is tightly cropped. JS and tikztosvg share a 114px width; the remaining 8px height/bbox variance and thin glyph/stroke rasterization are visible in the sheet, but no source polarity or component is missing. Artifacts: outputs/qa-circuitikz-basic-v4/{native-mactex,tikzkit-grid-png,tikztosvg-grid-png,diff}/circuitikz-basic-bipoles*. Read circuitikz.sty, pgfcircbipoles.tex, pgfcircvoltage.tex, and circuitikzmanual.tex. Controlled/battery sources, source rotations and mirrors, and the wider bipole catalog remain partial.

- tkz-euclide legacy reference compatibility now feeds both tikztosvg and MacTeX fallback inputs through the same temporary migration: TeX Live 2025 says \usetkzobj{all} is no longer necessary, and its old triple-angle macro appends cm itself, so historic size=0.6cm becomes the invalid 0.6cmcm. The original LaTeX-examples source remains untouched; only disposable reference input drops the obsolete loader and converts legacy ll/lll sizes to numeric centimeters. The native MacTeX, JS, and tikztosvg panels for the interior/exterior-angle triangle all visibly contain the green inner arcs, triple blue exterior arcs, extended triangle edges, points, and P/Q/R labels. JS and tikztosvg differ by 363 pixels (2.02%), confined to text/stroke antialiasing and a 1px height variance, not lost geometry. Artifacts: outputs/qa-tkz-exterior-angles-v1/{native-mactex,tikzkit-grid-png,tikztosvg-grid-png,diff}/. Read tkz-draw-eu-angles.tex, tkz-lib-eu-marks.tex, and TKZdoc-euclide-presentation.tex. Broader tkz-euclide construction families remain partial.

## Next implementation order

1. Native text baselines and glyph metrics for the remaining near-pass cases.
2. Exact snake normal interpolation through sharp corners and flattened Bezier curves; zigzag remains a separate approximation.
3. Broader tree edge-from-parent path variants beyond straight generated child edges.
