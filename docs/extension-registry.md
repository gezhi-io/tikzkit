# TikZ Extension Registry

Generated by `node scripts/build-extension-registry.js` from the merged core gallery.

## Scope

- Corpus: `core`
- Cases: 1561
- Entries: 142
- Packages: 84
- TikZ libraries: 51
- PGFPlots libraries: 3
- PGF libraries: 2

## Status Columns

- `implementationStatus`: `builtin`, `extension`, `partial`, or `unsupported`.
- `localSourceFound`: whether MacTeX/TeX Live can locate the source with `kpsewhich`.
- `localSourceReviewed`: `yes` only when we have actually inspected the local source/doc for the current implementation. `no` means source exists but still needs review. `not-found` means no local source was found.
- `cases`: core gallery case IDs using the declaration.

The complete machine-readable table is [extension-registry.csv](./extension-registry.csv).

## Recent Implementation Notes

- 2026-07-01 `datavisualization.formats.functions` repeated in-data labels:
  reviewed `/usr/local/texlive/2025/texmf-dist/tex/generic/pgf/frontendlayer/tikz/libraries/datavisualization/tikzlibrarydatavisualization.code.tex`
  around `new label visualizer`, `label in data/.code`, and
  `pin in data/.code`. TeX Live creates a new label visualizer for each
  repeated `label in data` or `pin in data` key rather than overwriting the
  previous one. Added `datavisualization-097` and `datavisualization-098`.
  TikZKit now preserves repeated `label in data` options as multiple plain
  data-label overlays and repeated `pin in data` options as multiple label
  overlays with separate leader edges on the same visualizer. The remaining
  gap is the full native label visualizer object graph and arbitrary
  `every pin edge` style routing.
- 2026-07-01 `datavisualization.formats.functions` legend matrix style
  follow-up: re-read `/usr/local/texlive/2025/texmf-dist/tex/generic/pgf/frontendlayer/tikz/libraries/datavisualization/tikzlibrarydatavisualization.code.tex`
  around `/tikz/data visualization/legend options/matrix node style`.
  TeX Live routes this key into the legend matrix node styling, while built-in
  styles such as `opaque` and `transparent` are thin wrappers around the same
  hook. Added `datavisualization-093`, covering
  `legend={matrix node style={fill=black!25}}`. TikZKit now emits one
  legend-background node for focused matrix node fills and sizes it from the
  legend sample/text rows, including TeX operator words such as `\log` when
  estimating the background width. A focused right-side matrix padding
  calibration keeps the gray background width near the local `tikztosvg`
  reference (`50.5pt`, about `1.78cm`) for the two-row `\log x` / `x/2`
  legend. Visual comparison against local `tikztosvg` confirms the gray matrix
  background is present and covers both legend rows.
  A follow-up tick pass below handles the same case's half-step y tick density;
  the remaining visual gaps are exact bbox placement and TeX glyph/baseline
  metrics.
- 2026-07-01 `datavisualization.formats.functions` non-clean scientific tick
  follow-up: re-read the same TeX Live file around `scientific axes`,
  `every ticks`, and the linear `standard about strategy`. The local
  `tikztosvg` output for `datavisualization-093` shows a half-step y tick
  sequence for the small negative-to-positive logarithmic range, while TikZKit
  previously rendered only `-1, 0, 1`. Added a regression for that exact
  source and now generate visible y tick labels
  `−1.5, −1, −0.5, 0, 0.5, 1`, matching the reference tick density. The
  bbox follow-up below handles the vertical hidden-bounds mismatch that remained
  after this tick fix.
- 2026-07-01 `datavisualization.formats.functions` boxed-axis bbox
  follow-up: inspected the JS and local `tikztosvg` SVG viewBoxes for
  `datavisualization-093`. The JS output was vertically taller because its
  invisible `axis bounds` path inherited the generic PGFPlots
  `0.32cm` top/bottom container padding, expanding the ordinary SVG viewBox
  beyond visible datavis ticks and labels. Non-clean datavis boxed axes now use
  a focused `0.07cm` top/bottom hidden-bounds margin, matching the physical
  tick extent. After regenerating the comparison sheet, JS normalized height
  dropped from `333px` to `312px` against tikztosvg's `308px`, and aligned diff
  no longer needs a vertical translation (`dy=0`). Remaining gaps are exact
  horizontal bbox placement and TeX glyph metrics.
- 2026-07-01 `datavisualization.formats.functions` inside legend matrix fill
  follow-up: read `/usr/local/texlive/2025/texmf-dist/tex/generic/pgf/frontendlayer/tikz/libraries/datavisualization/tikzlibrarydatavisualization.code.tex`
  around `/tikz/data visualization/legend options/every legend inside`,
  `opaque`, and `transparent`. TeX Live defines inside legends as `opaque=white`
  plus footnotesize labels; `opaque=<color>` expands to legend matrix styling
  with rounded corners and `fill=<color>`, while `transparent` expands to
  `matrix node style={fill=none}`. Added `datavisualization-094` and
  `datavisualization-095` to compare `opaque=yellow!30` and `transparent`
  against local `tikztosvg`. TikZKit now routes these keys through the same
  focused matrix background path used for `matrix node style={fill=...}`.
  Visual review confirms the yellow fill and no-fill variants match the
  reference semantics; remaining visible gap is the existing slight inside
  legend placement offset.

- 2026-07-01 `datavisualization.formats.functions` legend visualizer styling
  follow-up: read
  `/usr/local/texlive/2025/texmf-dist/doc/generic/pgf/pgfmanual-en-dv-stylesheets.tex`
  around "Advanced: Labels in Legends and Their Visualizers" and
  `/usr/local/texlive/2025/texmf-dist/tex/generic/pgf/frontendlayer/tikz/libraries/datavisualization/tikzlibrarydatavisualization.code.tex`
  around `visualizer in legend style`. TeX Live applies this key only inside
  the legend cell picture. TikZKit now parses
  `label in legend={..., visualizer in legend style={...}}` into legend-only
  sample styling, so `datavisualization-096` can show a blue plotted curve with
  a red, thicker legend sample without changing the curve itself. The remaining
  limitation is the broader native legend matrix/object pipeline.

- 2026-07-01 `datavisualization.formats.functions` label selector follow-up:
  re-read `/usr/local/texlive/2025/texmf-dist/tex/generic/pgf/frontendlayer/tikz/libraries/datavisualization/tikzlibrarydatavisualization.code.tex`
  around `visualizer label options`, `label in data`, and `pin in data`,
  plus `/usr/local/texlive/2025/texmf-dist/tex/generic/pgf/modules/pgfmoduledatavisualization.code.tex`
  around the `count` class. Native `index`, `pos`, and default `auto` select through a
  per-visualizer count object: the first data point has count `1`, and
  `pos=<fraction>` uses `fraction * max(count)` as the threshold, while
  `auto` uses `(visualizer order - 0.5) / total visualizers * max(count)`.
  TikZKit now
  uses one shared sampled-point selector for `label in data` and `pin in data`,
  supporting numeric `when=<attribute> is <number>`, `index=<n>`, and
  `pos=<fraction>` plus the implicit Section 84 `auto` distribution, while preserving the existing `when=x is ...` pin leader
  crossing behavior. Added `datavisualization-090` and
  `datavisualization-091` for explicit selectors and `datavisualization-092`
  for default auto labels. A follow-up visual pass against local `tikztosvg`
  reduced the plain `label in data` auto normal offset from the old oversized
  value to a focused `0.14cm` screen-space offset, which places the `2x`,
  `x^2`, and `x^3` default-auto labels much closer to the TeX reference.
  A later pass also mirrored TeX Live's default `every data set label`
  `node style={font=\pgfutil@font@small}` by emitting `font=\small` for
  plain `label in data` nodes, reducing the formula-size mismatch in
  `datavisualization-092`. A further `node[auto, at end]` calibration now
  applies a `0.35em` physical center compensation after the `0.14cm` normal
  offset, moving the default `2x`, `x^2`, and `x^3` labels left toward the
  local `tikztosvg` reference without changing the selected data point.
  Remaining gaps are arbitrary label style routing, exact TeX glyph
  metrics/baselines, and the complete native label visualizer object graph.
- `datavisualization.formats.functions` visualizer `ignore style sheets`: re-read `/usr/local/texlive/2025/texmf-dist/doc/generic/pgf/pgfmanual-en-dv-visualizers.tex` around `/tikz/data visualization/visualizer options/ignore style sheets` and `/usr/local/texlive/2025/texmf-dist/tex/generic/pgf/frontendlayer/tikz/libraries/datavisualization/tikzlibrarydatavisualization.code.tex` where the key clears the visualizer signal and decrements `\tikzdvvisualizercounter`. Added `datavisualization-089`, covering a three-line style-sheet sequence whose middle visualizer ignores style sheets but keeps its explicit `style={line width=1pt}`. TikZKit now skips style-sheet options for that visualizer and does not consume the style sequence slot, so the following visualizer uses the next native color/dash position.
- `datavisualization.formats.functions` projected legend coordinates: re-read TeX Live's outer legend definitions in `/usr/local/texlive/2025/texmf-dist/tex/generic/pgf/frontendlayer/tikz/libraries/datavisualization/tikzlibrarydatavisualization.code.tex`, especially `north east outside/.style={at={([xshift=.8em]data visualization bounding box.north east|- data bounding box.north)}, anchor=north west}` and the matching west/south variants. Added `datavisualization-088`, covering a direct `legend={anchor=north west, at={([xshift=.8em]data visualization bounding box.north east|- data bounding box.north)}}` placement; the braces are required by native TikZ for this complex coordinate. TikZKit now parses focused `|-` and `-|` projection coordinates inside legend `at`, composes them with simple `xshift`/`yshift`, and keeps the native-sized legend sample/text row geometry. Remaining gaps are nested arbitrary TikZ calc expressions, multiple transforms in one coordinate, and exact native legend matrix dimensions.
- `datavisualization.formats.functions` explicit legend anchors: re-read `/usr/local/texlive/2025/texmf-dist/doc/generic/pgf/pgfmanual-en-dv-stylesheets.tex` Section 84 around the example `legend={anchor=north west, at=(data visualization bounding box.north east)}`, plus `/usr/local/texlive/2025/texmf-dist/tex/generic/pgf/frontendlayer/tikz/libraries/datavisualization/tikzlibrarydatavisualization.code.tex` where `/tikz/data visualization/legend options/anchor` and `at` store the target matrix anchor. Added `datavisualization-087`, covering the manual-style placement of a four-entry legend whose north-west matrix anchor is attached to `data visualization bounding box.north east`. TikZKit now parses focused `at` coordinates for `data visualization bounding box.*`, `data bounding box.*`, `axis description cs:...`, `visualization cs:...`, and `data cs:...`, then lays out the legend sample/text rows from that anchor using TeX-sized `2em` sample width, `0.5em` text gap, and `1.1em` row spacing. Remaining gaps are exact native matrix dimensions, arbitrary TikZ calc expressions inside `at`, and full legend object routing.
- `datavisualization.formats.functions` legend label node styles: re-read `/usr/local/texlive/2025/texmf-dist/doc/generic/pgf/pgfmanual-en-dv-stylesheets.tex` Section 84 around `/tikz/data visualization/every label in legend`, `legend options/label style`, and `legend entry options/node style`, plus `/usr/local/texlive/2025/texmf-dist/tex/generic/pgf/frontendlayer/tikz/libraries/datavisualization/tikzlibrarydatavisualization.code.tex` where `label style` appends to `every label in legend` and `legend entry options/node style` appends to the legend text node styling. Added `datavisualization-086`, covering `legend={label style={node style=draw}}` and per-entry `label in legend={..., node style={circle, draw=red}}`. TikZKit now routes both global and per-entry node styles to lowered legend label nodes and applies compact datavis legend math metrics so framed formulas such as `$\log x$` and `$x/2$` size close to the local `tikztosvg` reference. Remaining gaps are exact TeX glyph outlines, exact legend matrix baseline/axis range alignment, and arbitrary legend text-node styling beyond the focused node-style option path.
- `datavisualization` visualize tick text: re-read `/usr/local/texlive/2025/texmf-dist/doc/generic/pgf/pgfmanual-en-dv-axes.tex` Section 82 around `tick text at low` and `tick text at high`, plus `/usr/local/texlive/2025/texmf-dist/tex/generic/pgf/frontendlayer/tikz/libraries/datavisualization/tikzlibrarydatavisualization.code.tex` where `\tikz@lib@dv@show@ticks` computes `\tikz@dv@min@tick` / `\tikz@dv@max@tick` before placing tick nodes. Added `datavisualization-085`, covering `x axis={visualize ticks={direction axis=y axis, major={low=0pt, high=4pt, tick text at high}}}`. TikZKit now places focused Cartesian tick labels at the requested low/high endpoint with the correct outward anchor instead of falling back to the default below/left axis-label placement. Remaining gaps are exact tick-text padding/stacking, odd/even tick-specific padding, diagonal tangent anchors, and non-Cartesian direction-axis tick labels.
- `datavisualization` visualize ticks: re-read `/usr/local/texlive/2025/texmf-dist/doc/generic/pgf/pgfmanual-en-dv-axes.tex` Section 82 around `visualize ticks`, `low`, `high`, and `tick length`, plus `/usr/local/texlive/2025/texmf-dist/tex/generic/pgf/frontendlayer/tikz/libraries/datavisualization/tikzlibrarydatavisualization.code.tex` around `\tikz@lib@dv@show@ticks`. Added `datavisualization-084`, covering `xy Cartesian` with `x axis={visualize ticks={direction axis=y axis, major={low=-4pt, high=4pt, style=red!50}}}`. TikZKit now treats tick `low/high` as physical offsets along the requested direction axis, applies the per-kind tick style, and suppresses tick labels unless `tick text at low/high` is requested, matching the focused local `tikztosvg` reference. Remaining gaps are repeated `visualize ticks` accumulation, exact tick-text padding/stacking, non-Cartesian tangent vectors, subminor ticks, and full native datavis object routing.
- `datavisualization.polar` low-level angle scaling examples: re-read `/usr/local/texlive/2025/texmf-dist/doc/generic/pgf/pgfmanual-en-dv-polar.tex` Section 85 around `/tikz/data visualization/axis options/degrees` and `/tikz/data visualization/axis options/radians`, plus `/usr/local/texlive/2025/texmf-dist/tex/generic/pgf/frontendlayer/tikz/libraries/datavisualization/tikzlibrarydatavisualization.polar.code.tex` for the low-level polar front-end keys. Added `datavisualization-082` and `datavisualization-083`, covering `new polar axes={angle axis}{radius axis}` with `radius axis={unit length=1cm}` and `angle axis={degrees}` / `angle axis={radians}`. TikZKit already projects these through the focused low-level polar path; the new web cases give them tikztosvg artifacts and 1cm grid comparisons. Remaining gaps are exact scatter mark glyphs, arbitrary angle-axis mappers, visible low-level axes/ticks when requested, and the full native polar object system.
- `datavisualization` manual legend entries: re-read `/usr/local/texlive/2025/texmf-dist/doc/generic/pgf/pgfmanual-en-dv-stylesheets.tex` Section 84 around `new legend entry` and `visualizer in legend`, plus `/usr/local/texlive/2025/texmf-dist/tex/generic/pgf/frontendlayer/tikz/libraries/datavisualization/tikzlibrarydatavisualization.code.tex` around `new legend entry`, `add entry`, and `render legend entry`. Added `datavisualization-081`, the manual spacer-entry example. TikZKit now parses focused `new legend entry={text=..., visualizer in legend={\draw[solid] (0,0) circle[radius=2pt];}}`, preserves option order relative to adjacent automatic visualizer entries, and renders the custom circle glyph in the same legend row/column model. Remaining gaps are arbitrary `visualizer in legend` TeX code, `setup`, exact native matrix metrics, custom legend object routing, and the full datavis survey/object pipeline.
- `datavisualization.polar` logarithmic angle axes: re-read `/usr/local/texlive/2025/texmf-dist/doc/generic/pgf/pgfmanual-en-dv-polar.tex` Section 85 and the frontendlayer polar library around scientific polar axes. Added `datavisualization-079`, the manual-style `scientific polar axes={right half clockwise, clean}` example using `angle axis={logarithmic, ticks={minor steps between steps=8, major also at/.list={2,3,4,5,15,20}}}` and `radius axis={ticks={some, style=red!80!black}}`. TikZKit now maps polar angle source values through `log10` before projecting into the right half-plane, parses focused `major also at/.list`, uses native about ticks for explicit polar radius `ticks=some`, propagates radius tick-label color only to radius labels, keeps angle labels black, and draws logarithmic minor angle grid rays plus outer minor tick marks. Verified against local `tikztosvg` artifacts for `datavisualization-079`; remaining gaps are exact label anchors/glyph metrics, skewed-basis polar grid arcs, logarithmic radius axes, and the full native polar axis object system.
- `datavisualization` visualize grid: re-read `/usr/local/texlive/2025/texmf-dist/doc/generic/pgf/pgfmanual-en-dv-axes.tex` Section 82 around `visualize grid`, plus `/usr/local/texlive/2025/texmf-dist/tex/generic/pgf/frontendlayer/tikz/libraries/datavisualization/tikzlibrarydatavisualization.code.tex` around `xy Cartesian`, `every major grid`, and `every minor grid`. Added `datavisualization-080`, the manual `xy Cartesian` clipped-minor-grid example. TikZKit now treats `xy Cartesian` as a lower-level axis system using data units as centimeters, separates `grid` value generation from `visualize grid` drawing, suppresses tick labels unless `visualize ticks` is requested, draws only the axis that has `visualize grid`, applies `minor={low=0.25, high=1.75, style=red!50}`, and uses the native-like half-width `0.2pt` minor grid stroke. Verified against local `tikztosvg` artifacts for `datavisualization-080`; remaining differences are mostly arrow-tip geometry, exact bbox alignment, repeated `visualize grid` accumulation, non-Cartesian direction-axis paths, and full tick-text node placement for `visualize ticks`.
- `datavisualization.polar` small clean angle labels: re-read `/usr/local/texlive/2025/texmf-dist/tex/generic/pgf/frontendlayer/tikz/libraries/datavisualization/tikzlibrarydatavisualization.polar.code.tex` around `scientific polar axes/clean`, where TeX Live sets `angle axis={padding=.5em}` and routes angle tick text through `tick text at high`. Added a focused regression for the Section 85 `scientific polar axes={clean, 0 to 90}` one-centimeter scatter example. TikZKit now gives small clean polar axes a `0.18cm` high-side angle-label offset so the `30^\circ`, `60^\circ`, and `90^\circ` labels sit outside the padded quadrant boundary. Verified against `datavisualization-073`, with `datavisualization-074` and `datavisualization-026` regenerated as non-clean / large-clean guard cases. Remaining gaps are exact TeX glyph outlines, exact clean radius-axis line placement, logarithmic radius axes, arbitrary custom polar mappers, and full native object routing.
- `datavisualization` data label `text colored`: re-read `/usr/local/texlive/2025/texmf-dist/doc/generic/pgf/pgfmanual-en-dv-stylesheets.tex` Section 84 around `every data set label/.append style={text colored}`, `label in data`, and `pin in data`, plus `/usr/local/texlive/2025/texmf-dist/tex/generic/pgf/frontendlayer/tikz/libraries/datavisualization/tikzlibrarydatavisualization.code.tex` around `new label visualizer`, `visualizer label options/node style`, and `text colored/.style={node style={text=visualizer color}}`. Added `datavisualization-078`, covering global and local data-label color inheritance for `label in data` and `pin in data`. TikZKit now propagates the active visualizer color to data-label text while keeping the pin leader edge stroke independent. The same pass fixed school-book pin placement by allowing `axis pin edge` / `axis label` overlays to overflow the data range clamp and by using a focused school-book leader-coordinate calibration against local `tikztosvg`. Remaining gaps are the full native label visualizer object graph, `pos`/`index`/arbitrary attributes, and arbitrary node-style routing beyond focused `sloped`/`text colored`.
- `datavisualization` named legends: re-read `/usr/local/texlive/2025/texmf-dist/tex/generic/pgf/frontendlayer/tikz/libraries/datavisualization/tikzlibrarydatavisualization.code.tex` around `new legend`, `every new legend`, `new legend entry`, and `legend entry options/legend`. TeX Live creates one legend object per name, routes `label in legend={..., legend=<name>}` through `new legend entry`, and then lays out each named legend as its own matrix. Added `datavisualization-077`, covering `new legend={upper legend}`, `new legend={lower legend}`, `upper legend=above`, `lower legend=below`, and per-entry `legend=upper/lower legend`. TikZKit now records the legend target on each plot and renders independent legend groups, so upper entries are placed above the frame and lower entries below instead of all sharing the default legend row counter. Remaining gaps are exact native matrix dimensions, custom named legend anchors beyond focused placement options, and the full PGF legend object pipeline.
- `datavisualization` label visualizer styling: re-read `/usr/local/texlive/2025/texmf-dist/doc/generic/pgf/pgfmanual-en-dv-stylesheets.tex` Section 84 around `label in data`, `visualizer label options/node style`, and the example using `node style=sloped`, plus `/usr/local/texlive/2025/texmf-dist/tex/generic/pgf/frontendlayer/tikz/libraries/datavisualization/tikzlibrarydatavisualization.code.tex` where `label in data` creates a label visualizer and passes node style through `/tikz/data visualization/node style`. Added `datavisualization-076`, covering `label in data={text={$p$},node style=sloped,when=x is 2}`. TikZKit now computes the local curve tangent in screen-space axis units and emits a rotated label node, so sloped data labels follow the plot direction instead of staying horizontal. Remaining gaps are the full label visualizer object graph, `pos`/`index`/arbitrary attributes, and arbitrary label style routing.
- `datavisualization` data-coordinate legend placement: re-read `/usr/local/texlive/2025/texmf-dist/doc/generic/pgf/pgfmanual-en-dv-stylesheets.tex` Section 84 around `at values`, `right of`, and related `... of` legend placement keys, plus `/usr/local/texlive/2025/texmf-dist/tex/generic/pgf/frontendlayer/tikz/libraries/datavisualization/tikzlibrarydatavisualization.code.tex` where these keys set `at={(visualization cs:#1)}`, choose an anchor, and apply `every legend inside`. Added `datavisualization-071` and `datavisualization-072`, covering `legend={at values={x=1,y=1}}` and `legend={right of={x=1,y=1}}`. TikZKit now maps the requested data point through the current Cartesian axis range into `axis description cs`, supports the focused `at values`, `right/left/above/below of`, and diagonal `... of` anchors, and draws the native-like opaque inside legend matrix background for non-text-only inside legends so plot lines are masked behind the legend. Remaining gaps are exact native legend matrix dimensions, custom matrix styling, and the complete PGF legend object pipeline.
- `datavisualization` local tick options: re-read `/usr/local/texlive/2025/texmf-dist/doc/generic/pgf/pgfmanual-en-dv-axes.tex` around `options at`, `no tick text at`, and the three `value as ...` tick syntaxes, plus `/usr/local/texlive/2025/texmf-dist/tex/generic/pgf/frontendlayer/tikz/libraries/datavisualization/tikzlibrarydatavisualization.code.tex` where `no tick text at` is shorthand for `options at={#1 as [no tick text]}`. Added `datavisualization-070`, covering `no tick text at=3` and `also at=(pi) as [{tick text padding=1ex}] $\pi$`. TikZKit now hides generated tick text through this shorthand while preserving the tick mark, keeps the custom `\pi` label, and uses a small boundary tolerance so sampled sine curves keep the native `1` top y-axis label. Remaining gaps are exact per-tick `tick text padding` geometry, local style/node-style application, and repeated `options at` accumulation.
- `datavisualization` manual tick/grid labels: reviewed `/usr/local/texlive/2025/texmf-dist/doc/generic/pgf/pgfmanual-en-dv-axes.tex` around manual tick placement and tick label syntax, plus `/usr/local/texlive/2025/texmf-dist/tex/generic/pgf/frontendlayer/tikz/libraries/datavisualization/tikzlibrarydatavisualization.code.tex` around `at`, `also at`, `major at`, and `major also at`. Added `datavisualization-069`, covering the PGF manual `x axis={ticks=many, ticks and grid={major also at={(pi/2) as $\frac{\pi}{2}$}}}` example. TikZKit now lowers `major at` as an override, `also at`/`major also at` as additive tick/grid positions, and preserves custom tick text in `xticklabels`/`yticklabels`. Remaining gaps are minor/subminor variants, exact repeated-key ordering, and local per-tick styling options beyond `no tick text`.
- `datavisualization` Cartesian axis scaling: reviewed `/usr/local/texlive/2025/texmf-dist/doc/generic/pgf/pgfmanual-en-dv-axes.tex` around `axis options/scaling=<source> at <target> and <source> at <target>` and `/usr/local/texlive/2025/texmf-dist/tex/generic/pgf/frontendlayer/tikz/libraries/datavisualization/tikzlibrarydatavisualization.code.tex` around the default `scaling mapper`. Added `datavisualization-068`, covering table data with non-`x/y` attributes, `x axis={attribute=people}`, and `y axis={attribute=year, scaling=1900 at 0cm and 2000 at 5cm}`. TikZKit now maps source values to target physical coordinates, uses the scaling target as the physical axis span, and keeps source tick labels while plotting at scaled positions. Remaining gaps are nonlinear `function=<code>` scaling, full native axis object/survey phases, repeated scaling-key order semantics, and arbitrary multi-attribute transformations.
- `datavisualization.formats.functions` legend calibration: re-read `/usr/local/texlive/2025/texmf-dist/tex/generic/pgf/frontendlayer/tikz/libraries/datavisualization/tikzlibrarydatavisualization.code.tex` around `/tikz/data visualization/legend options`, `south east outside`, `label in legend mark coordinates`, `straight label in legend line`, and `default label in legend path`. The implementation now distinguishes a user-explicit `legend={south east outside}` placement from the implicit default outside legend inserted for labeled visualizers, so scatter marks, explicit local mark coordinates, style-sheet legend rows, and school-book straight legend samples use their own native-like calibrated origins instead of all falling through the generic east-outside matrix placement. Verified against `datavisualization-001`, `datavisualization-019`, `datavisualization-051`, and `datavisualization-053` with local tikztosvg output; remaining visual gaps are mostly SVG-text/TeX glyph metrics and the broader school-book axes bounding-box offset.
- `datavisualization.sparklines`: reviewed `/usr/local/texlive/2025/texmf-dist/tex/generic/pgf/frontendlayer/tikz/libraries/datavisualization/tikzlibrarydatavisualization.sparklines.code.tex`. TeX Live defines `spark line` as a compact datavisualization style with `x axis={unit length={1pt}}`, a y band from `-0.2em` to `0.8em`, and the normal datavisualization pipeline; the source also marks sparklines as “not yet usable” because `new line plot` is not defined in this TeX Live install. Added `datavisualization-066`, covering a focused lowering of `spark line` into a compact tickless straight line plot with 1pt-per-x-unit width, a 1em vertical band, 0.4pt stroke width, and round cap/join paths. The tikztosvg reference runner now lowers this same unsupported native style into a plain TikZ reference path for comparison. Remaining gaps are full native sparkline object routing, exact TeX `em`/`ex` metrics, inline baseline integration, and arbitrary sparkline styles.
- `datavisualization.barcharts`: reviewed `/usr/local/texlive/2025/texmf-dist/tex/generic/pgf/frontendlayer/tikz/libraries/datavisualization/tikzlibrarydatavisualization.barcharts.code.tex` and `/usr/local/texlive/2025/texmf-dist/tex/generic/pgf/libraries/datavisualization/pgflibrarydatavisualization.barcharts.code.tex`. Added `datavisualization-065`, covering the focused `candle stick plot` path with `day` plus `dax/low`, `dax/high`, `dax/entry`, and `dax/exit` columns, compact native-like 3mm day spacing, native x-axis baseline tick `0` plus integer candle days, `y axis/source max=100`, separate wick/body paths, white rise bodies, and black fall bodies. Follow-up calibration keeps the clean x-axis boundary at the native `day * 3mm` transformer position instead of expanding the data range by the physical 4pt candle body width. A second calibration matches the local tikztosvg reference axis geometry for this focused lowering: clean x/y axes sit on the data origin, extend to `maxDay*3mm + 0.18cm` and `sourceMax/100cm + 0.08cm`, and use short `0.025cm` ticks plus `0.25pt` tick/boundary strokes. A third calibration tightens the invisible clean-axis bbox right edge from the generic PGFPlots margin to the tikztosvg physical bbox and switches generated `axis tick label` nodes to TeX digit metrics with no default inner sep, moving east-anchored y ticks into native positions. The TeX Live visualizer computes the 4pt body width as a canvas-position offset after transforming data points, so the body/wick path may extend beyond the clean data boundary; TikZKit now preserves that overflow for `axis candlestick body/wick` instead of clamping overlay coordinates to `xmax`/`ymax`. Remaining gaps are the full PGF object/signal pipeline, arbitrary custom `use path rise/fall/wick` hooks, exact SVG glyph outlines, and non-default candle axis transformers.
- `datavisualization.formats.functions` legend matrix follow-up: reviewed `/usr/local/texlive/2025/texmf-dist/tex/generic/pgf/frontendlayer/tikz/libraries/datavisualization/tikzlibrarydatavisualization.code.tex` around `legend options`, `main legend`, `columns`, `rows`, `max columns`, `max rows`, `right then down`, matrix creation, and the native `matrix node styling={row sep=0pt,column sep=.8em}`, plus `/usr/local/texlive/2025/texmf-dist/doc/generic/pgf/pgfmanual-en-dv-stylesheets.tex` Section 84 rows/columns. Added `datavisualization-063`, covering explicit east-outside `legend={right then down, columns=2}`, `datavisualization-064`, covering `legend={right then down, max columns=2}`, and `datavisualization-075`, covering TeX Live's directly compilable `legend={max rows=2}` form. The regression suite also covers TikZKit's focused `main legend={max rows=2}` entrypoint, which appears in the manual's legend-object path but requires native legend creation before TeX Live accepts it as a direct key. Legend entries now form bounded matrix layouts instead of falling back to one entry per row; `max rows` uses the documented down-then-right strategy and starts a new column when the current column would exceed the row limit. Follow-up calibration computes the matrix column offset from TeX-sized legend sample width, text gap, estimated label width, and `.8em` column separation; the row baseline now uses TeX Live's effective `1.1em` matrix row pitch instead of a fixed axis-description step, matching local tikztosvg rows for `columns=2`, `max columns=2`, and `max rows=2`. Compact clean `0..0.4` y axes use `0.1` ticks to match the same local tikztosvg reference. Remaining gaps are exact TeX matrix cell sizing, up/left strategy calibration, custom anchors, and full native legend object routing.
- `datavisualization.formats.functions` north-outside legend follow-up: reviewed `/usr/local/texlive/2025/texmf-dist/tex/generic/pgf/frontendlayer/tikz/libraries/datavisualization/tikzlibrarydatavisualization.code.tex` around `north outside/.style={rows=1, at=..., anchor=south}` and `/usr/local/texlive/2025/texmf-dist/doc/generic/pgf/pgfmanual-en-dv-stylesheets.tex` Section 84 legend placement. Added `datavisualization-067`, covering `legend=north outside` / `above` so entries appear above the data frame in one horizontal row instead of falling back to the default east-outside column. Remaining gaps are exact native matrix width, row baseline, and arbitrary custom anchors.
- `datavisualization` rectangle visualizers: reviewed `/usr/local/texlive/2025/texmf-dist/tex/generic/pgf/modules/pgfmoduledatavisualization.code.tex` around the `rectangle visualizer` class and `/usr/local/texlive/2025/texmf-dist/tex/generic/pgf/frontendlayer/tikz/libraries/datavisualization/tikzlibrarydatavisualization.code.tex` around `visualize as rectangles`, `attribute 1`, `attribute 2`, and `label in legend rectangle coordinates`. Added `datavisualization-061`, covering `visualize as rectangles/.list={cold,hot}`, table headers with `temp/min temp/max load/min load/max`, matching `x axis={attribute=temp}` / `y axis={attribute=load}`, per-visualizer stroke styles, and native-sized default rectangle legend samples. Remaining gaps are the complete native object/signal pipeline, arbitrary custom rectangle legend strategies, and exact TeX font/legend matrix sizing.
- `arrows` / `trees`: reviewed `/usr/local/texlive/2025/texmf-dist/tex/generic/pgf/frontendlayer/tikz/libraries/tikzlibrarytrees.code.tex`, `/usr/local/texlive/2025/texmf-dist/tex/generic/pgf/frontendlayer/tikz/libraries/tikzlibraryarrows.code.tex`, and `/usr/local/texlive/2025/texmf-dist/tex/generic/pgf/libraries/pgflibraryarrows.code.tex` for `library-packages-001`. The specific TCS-logo example now renders through a focused TeX-lite expansion of `\logo{...}` plus the declared `leaf` arrow into ordinary TikZ paths, preserving the four color variants, cyclic branch geometry, leaf tips, small-caps label, minipage/tabular stack, and source-grid overlay. This is not a complete `\pgfarrowsdeclare` engine or full `child foreach` tree interpreter; generic custom arrow declarations, fork edge paths, missing children, and full growth-function hooks remain partial.
- `datavisualization.formats.functions` inside legend follow-up: reviewed `/usr/local/texlive/2025/texmf-dist/tex/generic/pgf/frontendlayer/tikz/libraries/datavisualization/tikzlibrarydatavisualization.code.tex` around `every legend inside`, `south east inside`, and `text only`. Recalibrated focused `legend={south east inside, rows=2, label style=text only}` placement using local tikztosvg output for `datavisualization-045`: text-only labels now sit lower in the data bounding box and the opaque legend matrix no longer floats over the curves as high as before. Full native legend matrix layout remains partial.
- `datavisualization.formats.functions` follow-up: added `datavisualization-057` for Cartesian `x axis={attribute=angle}` / `y axis={attribute=radius}` function data, focused `minor steps between steps` minor grid/ticks, and `ticks={..., style=...}` tick-label color propagation. Also recalibrated default outside line/closed/gap legend samples to use native physical TeX offsets from the data frame (`1.5em` start gap, `2em` sample, `0.5em` label gap), which fixes short-axis Section 84 cases such as `datavisualization-055` and `datavisualization-056`; the physical offset is scoped to default outside legends so explicit below, inside, west-outside, and school-book legend placements keep their own calibrated geometry.
- `datavisualization.formats.functions`: reviewed `/usr/local/texlive/2025/texmf-dist/tex/generic/pgf/libraries/datavisualization/pgflibrarydatavisualization.formats.functions.code.tex`, `/usr/local/texlive/2025/texmf-dist/tex/generic/pgf/frontendlayer/tikz/libraries/datavisualization/tikzlibrarydatavisualization.code.tex`, `/usr/local/texlive/2025/texmf-dist/tex/generic/pgf/modules/pgfmoduledatavisualization.code.tex`, `/usr/local/texlive/2025/texmf-dist/tex/generic/pgf/math/pgfmathfunctions.random.code.tex`, `/usr/local/texlive/2025/texmf-dist/tex/generic/pgf/math/pgfmathfunctions.misc.code.tex`, `/usr/local/texlive/2025/texmf-dist/tex/generic/pgf/math/pgfmathfunctions.comparison.code.tex`, and `/usr/local/texlive/2025/texmf-dist/doc/generic/pgf/pgfmanual-en-dv-stylesheets.tex` for the manual function-format Gaussian/scatter/style-sheet cases. Added interval `step`, numeric grouped variables, multi-variable Cartesian product sampling, source-ordered `func` evaluation, two-pass survey/visualization sampling for function data, `\value <var>` function evaluation, deterministic PGF-style `rand`/`rnd`/`random(n)`, focused PGF math helpers including `veclen`, `ifthenelse`, `sinh`, `cosh`, and comparison predicates, nearest-point pin leader edges with approximate normal offset, screen-space `3ex` pin distance, and a separate `node[auto, at end]`-style text-center side offset, simple scatter legend example markers, native-like scatter mark color isolation so bare visualizer colors do not tint `mark=x` points in the PGFPlots lowering, native `mark=x` diagonal endpoint sizing via `mark size / sqrt(2)`, mixed line/scatter legend column alignment with native-sized `mark=x` legend samples, default zig-zag legend mark coordinates, `straight label in legend line` plus global `legend entry options/default label in legend path/.style=straight label in legend line` with plot marks at the native 25% and 75% sample positions, circular closed-path legend samples with TeX Live's two mark coordinates, native-like school-book outside line legend placement, native-size pin labels, outside legend labels inheriting TeX Live `every data set label` `\small`, west/east anchored legend labels preserved through SVG-text math fallback, TeX-like minus glyphs for generated negative tick labels, SVG-text inline sum side scripts plus explicit `\sum\limits` stacked limits, SVG-text upright named math operators such as `\sin`/`\cos`, math minus scripts, and TeX-like spacing after scripted atoms in datavis legend and pin labels, nested `scientific axes={clean,end labels}` parsing with positive-end x/y labels, nested `scientific axes={clean,upright labels}` with west-side unrotated y labels, TeX Live `thin`/default major grid/tick stroke widths for school-book/clean axes, native-like quarter y ticks for clean `-1:1` and `0:2` ranges, clean single-point scatter origin-only ticks, TeX Live standard about-step ticks for `few`/`some`/`many`/`about=<n>` with boundary snapping for function-sampling floating error, below legends with row-count layout, `style sheet=strong colors/vary hue/vary dashing/vary thickness/vary thickness and dashing` for line visualizers, and the TeX Live Chapter 83 visualizer handler options `straight cycle`, `smooth cycle`, and `polygon` as closed axis outlines, plus `gap line` and `gap cycle` as shortened independent axis segments with five-point gap circular legend samples, and focused inside legend `label style=text only` placement with a single opaque legend matrix background, `label style=text colored` label coloring, scatter `label in legend three marks` samples, and simple scatter `label in legend mark coordinates={...}` coordinate lists, and west-outside text-left scatter legend mirroring. Remaining gaps are the complete native object/signal pipeline, exact TeX `em`/`ex` metrics, exact minor/subminor grid variants, full label visualizers, native matrix legend layout, and complete pgfmath.
- `datavisualization.polar`: reviewed `/usr/local/texlive/2025/texmf-dist/tex/generic/pgf/frontendlayer/tikz/libraries/datavisualization/tikzlibrarydatavisualization.polar.code.tex`, `/usr/local/texlive/2025/texmf-dist/tex/generic/pgf/libraries/datavisualization/pgflibrarydatavisualization.polar.code.tex`, and `/usr/local/texlive/2025/texmf-dist/doc/generic/pgf/pgfmanual-en-dv-polar.tex` for PGF manual section 85. Added a focused `scientific polar axes` slice that projects function-format `angle`/`radius` data into ordinary TikZ coordinates, emits half-plane and full-circle polar grid arcs/rays, handles braced `\value{angle}r`, common fixed polar ranges, smooth line visualizers, clean outer padding/boundary circles, explicit below legends, default outside-right legends aligned to the 0-degree radius line for full-circle axes, and duplicate 360-degree endpoint tick suppression. Added non-clean `outer ticks` / `inner ticks` support so the black!50 angle-axis boundary, focused half-plane radius axes, symmetric radius ticks, radius tick labels, angle tick labels, and inward/outward tick direction render even without `all axes=grid`; the outer angle labels now sit near the tick tip instead of floating far beyond the arc, matching the `datavisualization-048` and `datavisualization-074` tikztosvg references more closely. Added the official low-level `new polar axes={angle axis}{radius axis}` named-data scatter path: `data [format=named]` expands list/range attribute assignments as cartesian products, low-level angles use direct-degree mapping, custom `angle axis={unit vectors={...}{...}}` transforms scatter coordinates, pure low-level axes no longer emit default ticks/axes unless grid or ticks are requested, and scatter output is mark-only rather than a connected path. Added `datavisualization-062` for `radius axis={ticks=none}` so explicit empty radius ticks suppress the default quarter-ring fallback. Added `datavisualization-073` and `datavisualization-074` for the manual scientific-polar explicit scatter examples: `data point [angle=..., radius=...]` now survives preprocessing, degree tick suffixes render as math labels, `radius axis={length=...}` is treated as the total physical axis span with data scaled by the surveyed radius max unless `max value` is explicit, clean partial ranges draw pale endpoint radius axes plus `.5em` outward-offset clean boundary axes and endpoint radius labels, and non-clean radius ticks straddle their axes like the tikztosvg reference. Remaining gaps are exact TeX glyph metrics and some radius-label placement details, logarithmic radius axes, arbitrary custom polar mappers, skewed basis grid/arc geometry, multi-row/nondefault legend matrix layout, and the full polar datavisualization object system.
- 2026-07-01 polar tick-label calibration: re-read the frontendlayer polar setup for `visualize ticks={..., major={tick text at high}}` and the generic tick text padding keys in `tikzlibrarydatavisualization.code.tex`. TikZKit now scales the extra JS-side polar tick-label padding by `outerRadius` for small scientific polar charts instead of using fixed `0.12cm` / `0.24cm` offsets. This moves the `datavisualization-073` and `datavisualization-074` angle/radius labels closer to tikztosvg while preserving the existing large-radius polar tests. Remaining visible mismatch is mostly SVG text glyph box/anchor metrics, especially the clean quadrant `90^\circ` label.
- 2026-07-01 polar math tick-label follow-up: traced the clean-quadrant `90^\circ` mismatch through generated TikZ, IR, and SVG. The polar coordinate was close; the remaining jump came from treating math-only `axis tick label` nodes like ordinary text nodes with `.3333em` inner sep. Pure math axis tick labels now use compact inner-sep metrics unless `inner sep` is explicit, moving `datavisualization-073`/`074` degree labels closer to tikztosvg without changing ordinary nodes or plain tick labels. Remaining visible mismatch is mostly exact SVG glyph outlines and side anchors for `0^\circ`/`30^\circ`/`60^\circ` labels.
- 2026-07-01 polar shallow-angle label anchor follow-up: re-read the `scientific polar axes/clean` `visualize ticks={radius axis={goto=padded max}, ..., major={tick text at high}}` setup in `tikzlibrarydatavisualization.polar.code.tex` and compared `datavisualization-073` against local tikztosvg crops. TikZKit now anchors first-quadrant shallow angle labels (`10^\circ..45^\circ`) on the west/high side instead of centering them with `south`, moving the `30^\circ` label to the outside-right side of the tick like the TeX reference. Remaining mismatch is dominated by exact TeX glyph metrics and overall normalized bbox alignment rather than missing polar geometry.
- 2026-07-01 clean half-plane polar follow-up: re-read `scientific polar axes/clean` in the frontendlayer source. Native clean polar ticks place angle labels through `visualize ticks={radius axis={goto=padded max}, ..., major={tick text at high}}`, so labels need a small high-side offset beyond the padded boundary. TikZKit now applies a scaled clean angle-label padding (`outerRadius * 0.032`, capped) so the `datavisualization-026` `\frac{1}{2}\pi` label sits near the tikztosvg outer arc while preserving the small-radius `datavisualization-073`/`074` cases.
- 2026-07-01 large clean half-plane polar follow-up: compared `datavisualization-026` against local `tikztosvg` glyph coordinates. The previous positive clean angle-label padding double-counted outside placement because native `tick text at high` plus `anchor=south` already moves the text outside the padded arc. Large clean half-plane polar charts now use a small negative coordinate padding for angle labels, moving `\frac{1}{2}\pi` from `y=3.8665cm` into the native glyph-bbox neighborhood (`3.62..3.76cm`) while leaving the small-radius `datavisualization-073`/`074` tests unchanged.
- 2026-07-01 polar below-legend follow-up: re-read `legend=below` / `south outside` in `tikzlibrarydatavisualization.code.tex`. Native placement anchors the legend matrix below `data visualization bounding box.south` with `yshift=-.5em`, so the row must account for radius tick-label extent rather than sit just under the radius axis. TikZKit now moves the clean half-plane polar legend row from `-1.02cm` to the native south-outside neighborhood (`-1.28cm`) for `datavisualization-026`; remaining mismatch is exact TeX glyph metrics and full legend matrix sizing.
- `chronology`: reviewed `/usr/local/texlive/2025/texmf-dist/tex/latex/chronology/chronology.sty` for Case 521. Added a focused package preprocessor that expands `chronology` environments in beamer frames into ordinary TikZ timelines, including `[step]{start}{stop}{width}{unit}`, `\event` point/span events, `\decimaldate` arithmetic, an approximate beamer title bar, and no-op `appendixnumberbeamer` compatibility. Remaining gaps are exact beamer/metropolis page layout, chronology `\eventspan`/`\eventpoint`, and precise TeX font metrics.
- `shadings`: reviewed `/usr/local/texlive/2025/texmf-dist/tex/generic/pgf/frontendlayer/tikz/libraries/tikzlibraryshadings.code.tex` and `/usr/local/texlive/2025/texmf-dist/tex/generic/pgf/libraries/pgflibraryshadings.code.tex` for Case 286. Added a focused custom radial shading slice: `\pgfdeclareradialshading{name}{...}{color(...)=(...)}` is collected from the source, `shading=<name>` maps shaped nodes to a radial SVG gradient, and transparent PGF stops now render the missing atom sphere in `thomson-scattering`. Remaining gaps are `fadings`, functional shadings/color wheels, and exact PGF shading transforms.
- `fadings`: reviewed `/usr/local/texlive/2025/texmf-dist/tex/generic/pgf/frontendlayer/tikz/libraries/tikzlibraryfadings.code.tex` and `/usr/local/texlive/2025/texmf-dist/tex/generic/pgf/libraries/pgflibraryfadings.code.tex` for Case 456. Added a focused axial path fading slice for `path fading=west/east/north/south`: options preserve the fading name and SVG output now emits object-bounding-box masks matching PGF's 25%-75% transparent-to-opaque ramps. Case 456's connection-strength legend now fades instead of rendering as a hard solid bar. Remaining gaps are custom `\tikzfading`, radial `fade out`, image/mask-backed exact PGF fadings, and colorlet-driven fill color fidelity.
- `decorations.pathreplacing` / inline path labels: reviewed `/usr/local/texlive/2025/texmf-dist/tex/generic/pgf/libraries/decorations/pgflibrarydecorations.pathreplacing.code.tex` and `/usr/local/texlive/2025/texmf-dist/tex/generic/pgf/frontendlayer/tikz/libraries/tikzlibrarydecorations.pathreplacing.code.tex` for Case 214. The PGF brace path was already close; the visible gap came from inline `node[midway]` labels inheriting picture-level `draw=white` as node boxes. Inline path labels now inherit text color without drawing boxes from inherited path/picture `draw`/`fill`, and SVG text math fallback now renders `\neg`/`\lnot` as `¬`.
- `shadows.blur`: reviewed `/usr/local/texlive/2025/texmf-dist/tex/latex/pgf-blur/tikzlibraryshadows.blur.code.tex` for Case 387. Mapped `blur shadow` defaults (`shadow xshift=.5ex`, `shadow yshift=-.5ex`, `shadow blur radius=.4ex`, `shadow opacity=40`) onto the existing node shadow IR and SVG Gaussian blur filters, and added missing SVG/xcolor names used by the case (`LightSteelBlue`, `DimGray`). Remaining gaps are path-level blur preactions, pgf fading geometry, and exact text/list parsing.
- `pgfgantt`: reviewed `/usr/local/texlive/2025/texmf-dist/tex/latex/pgfgantt/pgfgantt.sty` for Case 690. Added a closer `ganttchart` expansion model where `\\` drives row advancement, adjacent `\gantttitle` cells share a title row, `vgrid`/`hgrid` stay disabled unless explicit, inline labels move inside bars/groups, `bar/.append style={fill=...}` is honored, and ordinary TeX accent labels such as `Trajet\'oria acad\^emica` render as Unicode text. Remaining gaps are exact pgfgantt custom shapes, group peak geometry, milestone label offsets, and font metrics.
- `pgfplotslibrary:groupplots` / nested mini TikZ: reviewed `/usr/local/texlive/2025/texmf-dist/tex/generic/pgfplots/libs/tikzlibrarypgfplots.groupplots.code.tex` for Case 200. Groupplots source positions child axes through named anchors such as `group c<col>r<row>`; the current JS gap is still axis frame/sizing, but this pass fixed nested `\scalebox{...}{\begin{tikzpicture}...}` unit-cell overlays by preserving wrapper scale, parsing projected 3D coordinates/polylines, and honoring `ball color`/atom size so Case 200 no longer falls back to gaussian placeholders.
- `pgfplots`: reviewed `/usr/local/texlive/2025/texmf-dist/tex/generic/pgfplots/pgfplotsmeshplothandler.code.tex` for Case 179. Added axis-level `surf` handling for `\addplot3 coordinates`, preserving z values and expanding 2x2/mesh coordinate grids into filled 3D surface patches with explicit plot colors and opacity. Remaining visual gap is PGFPlots default 3D camera/axis layout alignment.
- `pgfplots`: reviewed `/usr/local/texlive/2025/texmf-dist/tex/generic/pgfplots/pgfplots.code.tex` and `/usr/local/texlive/2025/texmf-dist/tex/generic/pgfplots/pgfplots.scaling.code.tex` for Case 587. Added a `unit vector ratio*` boxed-axis sizing slice: one explicit dimension first derives the missing dimension from PGFPlots' default 240pt/207pt aspect, then fits the plot area into the label-adjusted target box; Case 587 now matches the native/tikztosvg axis frame size within about 0.03cm. Remaining gaps are plot/text styling and Case 179 3D camera alignment.
- `xcolor`: reviewed `/usr/local/texlive/2025/texmf-dist/tex/latex/xcolor/xcolor.sty` and `/usr/local/texlive/2025/texmf-dist/tex/latex/xcolor/svgnam.def` for Cases 180 and 456. Added SVG/xcolor named RGB values used by `svgnames`, so mixes such as `DarkRed!10` and `DarkBlue!10` resolve to pale fills instead of falling back to the saturated base color. Added a focused `\colorlet{name}{base!p!other}` alias slice so Case 456's `\colorlet{mewnol}{blue!75!cyan}` connection-strength legend renders blue instead of black while retaining path fading.
- `forest`: reviewed `/usr/local/texlive/2025/texmf-dist/tex/latex/forest/forest.sty` and `/usr/local/texlive/2025/texmf-dist/doc/latex/forest/forest-doc.tex` for Case 172. Added an extension that expands practical `forest` bracket trees into ordinary TikZ nodes, edges, and midpoint branch labels, including `content:branch` splitting and `l sep`/`s sep` layout hints used by branch-and-bound examples.
- `neuralnetwork`: reviewed `/usr/local/texlive/2025/texmf-dist/tex/latex/neuralnetwork/neuralnetwork.sty` for Case 166. Added a package extension that expands `neuralnetwork` into ordinary TikZ nodes/links, covering network height/layer spacing/node spacing, layer titles, `\inputlayer`/`\hiddenlayer`/`\outputlayer`, text callbacks from TeX-lite macros such as `text=\xin`, and full `\linklayers` connections.
- `stanli`: reviewed `/usr/local/texlive/2025/texmf-dist/tex/latex/stanli/stanli.sty` for Case 1561 and expanded the practical 3D structural slice covering `\dbeam` endpoint dots, `\dsupport` direction anchors, `\dhinge{2}` hinge circles/connectors, `\dlineload{5}` distributed loads, and plane-aware `\ddimensioning`.
- `tqft`: reviewed `/usr/local/texlive/2025/texmf-dist/tex/latex/tqft/tikzlibrarytqft.code.tex` for Case 178. Added a core partial compatibility slice for option-only `\pic[...]` TQFT cobordisms, `every tqft` style merging, rotated boundary anchors, native-like adjacent `between incoming/outgoing ...` interior anchors, open mixed cobordism outlines, and open half-arc boundary components; exact `tqft@fullpath` / `tqft@blist` curves remain the next visual gap.
- `chains`: reviewed `/usr/local/texlive/2025/texmf-dist/tex/generic/pgf/frontendlayer/tikz/libraries/tikzlibrarychains.code.tex` for Case 454. Added TikZ-like chain aliases (`<chain>-<n>`) plus `join=by ...` after-node paths so chain nodes emit the missing `x -> w` open-circle/latex arrows; branch/continue-chain helpers remain pending.
- `shapes.arrows`: reviewed `/usr/local/texlive/2025/texmf-dist/tex/generic/pgf/frontendlayer/tikz/libraries/tikzlibraryshapes.arrows.code.tex` and `/usr/local/texlive/2025/texmf-dist/tex/generic/pgf/libraries/shapes/pgflibraryshapes.arrows.code.tex` for Cases 434/515/516. Added partial `single arrow` and `double arrow` node shapes with arrow major-axis sizing from `minimum height`, thickness from `minimum width`, `single/double arrow head extend`, `single/double arrow head indent`, `shape border rotate`, and core tip/tail/head anchors. Remaining gaps are exact PGF arrow polygon formulas and specialized arrow-box helpers.
- `tikz plot function`: reviewed `/usr/local/texlive/2025/texmf-dist/tex/generic/pgf/frontendlayer/tikz/tikz.code.tex`, `/usr/local/texlive/2025/texmf-dist/tex/generic/pgf/modules/pgfmoduleplot.code.tex`, and `/usr/local/texlive/2025/texmf-dist/tex/generic/pgf/libraries/pgflibraryplothandlers.code.tex` for Cases 803/804. Added core parsing and JS sampling for `plot function{...}` so function-body parentheses are no longer misread as coordinates; expressions now cover `exp`, `gamma`, PGF-style `**`, variables/macros, domain/samples, scoped transforms, and `ycomb`/`xcomb` baseline stems. Remaining gap is exact native gnuplot table parity.

## Highest-Priority Unsupported Entries

| kind | name | cases | local source | reviewed | next note |
| --- | --- | ---: | --- | --- | --- |
| tikzlibrary | tikzmark | 311 | partial | yes | Reviewed tikzmark source; implemented formula-node `\tikzmarknode{name}{text}` cleanup plus approximate hidden anchors for Case 654; full `pic cs`, cross-picture remember-position, and overlay matrix highlighting remain pending |
| package | verbatim | 50 | found | no | Needs package compatibility pass |
| package | preview | 33 | found | no | Needs package compatibility pass |
| package | geometry | 30 | found | no | Needs package compatibility pass |
| package | inputenc | 20 | found | no | Needs package compatibility pass |
| package | babel | 10 | found | no | Needs package compatibility pass |
| tikzlibrary | shapes.geometric | 10 | partial | yes | Reviewed PGF shapes.geometric source; implemented simple .pic expansion with pic actions plus isosceles triangle node rendering/anchors and scalar x/y basis dimensions for Case 542; remaining geometric shapes still pending |
| tikzlibrary | 3d | 8 | found | no | Needs tikzlibrary compatibility pass |
| package | contour | 7 | found | no | Needs package compatibility pass |
| tikzlibrary | shadings | 7 | partial | yes | Case 286 custom `\pgfdeclareradialshading` radial stops now render `shading=<name>` atom nodes; fadings/functional shadings still pending |
| tikzlibrary | shadows.blur | 7 | partial | yes | Reviewed pgf-blur source; node `blur shadow` now renders as soft SVG filter shadows for Case 387; path preaction blur still pending |
| package | fontenc | 6 | found | no | Needs package compatibility pass |
| package | latexsym | 6 | found | no | Needs package compatibility pass |
| tikzlibrary | fadings | 6 | partial | yes | Case 456 axial `path fading=west/east/north/south` SVG masks; custom/radial fadings still pending |
| package | graphicx | 5 | found | no | Needs package compatibility pass |
| package | tkz-graph | 5 | found | no | Needs package compatibility pass |
| tikzlibrary | shapes.arrows | 5 | partial | yes | Reviewed PGF shapes.arrows source; implemented `single arrow`/`double arrow` node polygons, sizing, head extend/indent, `shape border rotate`, and core anchors for Cases 434/515/516; exact arrow-box helpers still pending |


## Highest-Priority Partial Entries

| kind | name | cases | local source | reviewed | next note |
| --- | --- | ---: | --- | --- | --- |
| package | circuitikz | 486 | found | yes | Case 869/871/872 circuitikz slice: short wires, R resistor, isource, terminal open/dot markers, op amp +/-/out anchors, ground, i/i>_/f/f>_/l/v labels; broader circuitikz still pending |
| package | pgfplots | 371 | found | yes | Case 802 `\pgfplotsinvokeforeach` plus parametric `\addplot ({...},...)` curves/fills now render; Case 587 boxed-axis sizing improved; remaining: axis plot-box size/offset, text styling, and Case 179 3D camera alignment |
| tikzlibrary | decorations.pathreplacing | 339 | found | yes | brace path replacement; mirror; raise; amplitude; aspect |
| tikzlibrary | mindmap | 332 | found | yes | concept/root/level styles; concept color; grow cyclic; clockwise/counterclockwise from; sibling angle |
| tikzlibrary | shadows | 323 | found | yes | general shadow; shadow xshift/yshift; shadow scale; node shadow rendering |
| tikzlibrary | intersections | 321 | found | yes | name path; name intersections; by; total; line/flattened curve intersections |
| tikzlibrary | trees | 318 | found | yes | node child trees; grow direction; level distance; sibling distance |
| tikzlibrary | decorations.text | 316 | found | yes | text along path; text={|style|content}; raise; midpoint tangent labels |
| package | pgfplotstable | 314 | found | no | \pgfplotstableread table data usable by addplot table |
| tikzlibrary | spy | 313 | found | yes | spy using outlines; \spy on ... in node ... at ...; connect spies; clipped magnified simple paths |
| pgfplotslibrary | groupplots | 312 | found | yes | Case 200 reviewed groupplots anchor placement source; nested scalebox unit-cell overlays now render as mini TikZ graphics; axis frame/sizing still pending |
| package | pgfcalendar | 311 | found | yes | Package compatibility plus TikZ calendar date expansion via calendar library subset |
| tikzlibrary | chains | 9 | found | yes | Case 454 chain aliases plus `join=by ...` after-node paths; branch/continue-chain helpers still pending |
| tikzlibrary | calendar | 311 | found | yes | \calendar dates, week list sunday, month label, day anchors, equals/between/weekend if styles |
| package | pgfgantt | 311 | found | yes | Case 690 row-aware ganttchart expansion; inline bars/groups/milestones and per-bar fill styles; exact shapes still partial |
| tikzlibrary | plotmarks | 311 | found | yes | mark=x; mark=+; mark=*; mark=o; square/triangle subset; mark size |
| package | amsmath | 38 | found | no | Many formulas are delegated to KaTeX; TeX macro package itself is not interpreted |
| package | mathtools | 23 | found | no | Formula display delegated to KaTeX; package-level commands are not complete |
| package | amssymb | 14 | found | no | Symbols mostly delegated to KaTeX or SVG text fallback |
| package | bm | 5 | found | no | \bm is normalized for common math labels |
| package | etoolbox | 4 | found | no | newtoggle/toggletrue/togglefalse/iftoggle subset |
| package | pgf | 4 | found | no | Core PGF-style path/color/math compatibility only |
| package | pgfmath | 4 | found | no | \pgfmathsetmacro and common expression subset |


## Implementation Workflow

1. Pick the highest-impact unsupported or partial entry by case count and visual severity.
2. Open `localSource` and `localDoc` when available, then record `localSourceReviewed=yes` only after reading the relevant macros/algorithm.
3. Add or update a focused extension file under `src/extensions/` for third-party packages, or a focused core module for built-in TikZ/PGF libraries.
4. Add a minimal unit test and at least one gallery/corpus visual case.
5. Regenerate this registry with `node scripts/build-extension-registry.js` and update the status/notes if the implementation changed.
